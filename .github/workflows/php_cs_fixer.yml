on: pull_request

name: Inspections
jobs:
    checkPHPErrors:
        name: Check PHP Error
        runs-on: ubuntu-latest
        steps:
            -   name: PHP Syntax Checker (Lint)
                uses: overtrue/phplint@8.0
                with:
                    path: .
                    options: --exclude=*.log

